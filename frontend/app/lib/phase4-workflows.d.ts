import type { CalculationAreaClassification } from "./types";
export function validateCalculationAreaDraft(points: Array<[number, number]>, settings: { name: string; classification: CalculationAreaClassification; calculation_plane_elevation_m: number | string; grid_spacing_m: number | string; maintenance_factor: number | string }): { name: string; classification: CalculationAreaClassification; calculation_plane_elevation_m: number; grid_spacing_m: number; maintenance_factor: number; wgs84_coordinates: Array<[number, number]> };
export function staleCalculationState(previous: { polygon_revision: number }, redraw: boolean): { status: "not-calculated"; polygon_revision: number; last_calculated_at: null; warnings: never[]; assumptions: never[]; provenance: Record<string, never> };
