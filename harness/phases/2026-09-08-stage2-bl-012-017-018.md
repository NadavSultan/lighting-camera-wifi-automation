# Work record — 2026-09-08 — Stage 2 BL-012 / BL-017 / BL-018

Status: implementation complete awaiting QA (implementer PASS; independent QA not this session)

## Scope, authority, and non-goals

- Requested work: Authorized Stage 2 items BL-012, BL-017, and BL-018 only.
- Controlling documents: `docs/superpowers/plans/2026-09-08-stage2-bl-012-017-018.md`; `docs/web-app-review-2026-09-08.md` WA-05, WA-10, WA-11; `docs/post-roadmap-backlog.md`; `docs/post-roadmap-stage1-p1-scoped-master-decision-2026-09-08.md`.
- Acceptance IDs governed by those documents: WA-05/BL-012, WA-10/BL-017, WA-11/BL-018.
- Authorized file boundary: frontend CSS/components/lib/tests for the three items; harness work record, execution log, and verification summary; backlog status notes only.
- Base commit and starting worktree status: `2f0f4a9c42a3a7bdbb51971591aaced5c57a8d38` on `codex/bl-006-ies-associations`. Starting diff: untracked controlling plan `docs/superpowers/plans/2026-09-08-stage2-bl-012-017-018.md` only (user-owned; preserved). No other uncommitted product files.
- Non-goals and later-phase exclusions: BL-013, BL-014, BL-016, BL-019, BL-020, OBS-01; merge, push, Phase 8, any seal; backend engines, schemas, `Input/`, catalogs, lockfiles; invented CAP operational values; persisting panel/card layout in project JSON; BL-018 shell rewrite.
- Durable goal identifier/state: Cursor `CreateGoal` created and activated 2026-09-08 — complete BL-012/017/018 with recorded verification on this checkout.
- Verifiable implementation stopping condition: `harness/verify/2026-09-08-stage2-bl-012-017-018-verification.md` records PASS for all three items and frontend test/typecheck/lint/build pass. Independent QA and merge are not this session’s stopping condition.

Do not copy requirements from the controlling contract. Link them and record only execution-specific interpretation or unresolved conflict.

Execution-specific notes (not requirement restatement):

- BL-012: confirm the card-anchor coordinate chain by measurement; do not treat the double-offset hypothesis as proven until measured.
- BL-018: discipline switcher only (Lighting / Camera / Wi-Fi / CAP), default Lighting after a project is open; not a three-column shell rewrite.

## Environment and file-boundary preflight

| Preflight item | Evidence | Result | Required path/configuration | Authorized? |
|---|---|---|---|---|
| Runtime discovery | `.venv\Scripts\python.exe --version` → Python 3.12.10; `node --version` → v24.19.0; `corepack pnpm --version` → 12.3.4 | pass | `.venv/Scripts/python.exe`; `frontend/package.json` engines `>=22.13.0` | yes |
| Locked dependency materialization | `frontend/node_modules` present; `frontend/pnpm-lock.yaml` and `backend/requirements.lock` present | pass | lockfiles exist; no lockfile edits | yes (no lockfile edits) |
| Build/test/lint/typecheck commands | `frontend/package.json` scripts `test`, `typecheck`, `lint`, `build` | pass | `frontend/package.json` | yes |
| Generated-artifact/validator commands | Stage 2 presentation only; no schema export | n/a | none | yes |
| Browser/rendered-QA runtime and ports | 3000/8000 occupied (existing local servers); 3010/8010 free for isolated live checks | pass | local Vinext + FastAPI when live-checking | yes |

Product changes must not begin until every required supporting path is either inside the authorized boundary or covered by an explicit recorded amendment.

## Milestones

| Milestone | Contract reference | Expected evidence | Status |
|---|---|---|---|
| M0 identity, preflight, reproduce | Stage 2 contract | this record + execution log | complete |
| M1 BL-012 card anchor | WA-05 | measured coordinate chain + clamp/offset tests | complete |
| M2 BL-017 tiny lux | WA-10 | formatLux policy tests + card/summary/label use | complete |
| M3 BL-018 left-panel switcher | WA-11 | switcher + overflow CSS/tests | complete |
| M4 Stage 2 verify | contract verification | harness verify + frontend suite | complete |

## Acceptance criteria

| Acceptance ID / criterion | Verification method | Evidence location | Status |
|---|---|---|---|
| BL-012 / WA-05 | coordinate-chain test + live card-to-anchor measure | harness/verify/2026-09-08-stage2-bl-012-017-018-verification.md | PASS (implementer) |
| BL-017 / WA-10 | formatLux cases + card/summary/label wiring | same | PASS (implementer) |
| BL-018 / WA-11 | switcher source/CSS + overflow-x; live 1920/1024 | same | PASS (implementer) |

## Changes and evidence

- Changed files: frontend CSS/components/lib/tests for the three items; harness work record, execution log, verification summary/live runner; backlog Stage 2 status notes
- Decisions applied: work in this checkout; card uses container-relative `map.project` plus 12 px; lux policy `<0.01 lx`; BL-018 is a four-tab switcher, not a shell rewrite
- Commands and durable evidence: `harness/logs/2026-09-08-stage2-bl-012-017-018-execution.md`; `harness/verify/2026-09-08-stage2-bl-012-017-018-verification.md`

## Verification requirements

- Required deterministic commands: from `frontend/`, `corepack pnpm run typecheck`, `lint`, `test`, `build`
- Required generated-artifact freshness checks: none (no schema/catalog change)
- Required source/hash preservation checks: do not modify `Input/`
- Required rendered or manual workflows: Miracle Mile 74-pole import SHA-256 `2f89f9f2be306c18221c643c98d5c1a9abdb6449aab8a77ea4b76b3694e8e328`; card anchor measurement; tiny-lux presentation if reproducible; left-panel switcher and overflow
- Required regression scope: existing frontend suite remains green

Record executions in a dated file made from `execution-log-template.md`; summarize the exact sealed commit in a verification record.

## Definition of Done

- [x] All authorized milestones are complete.
- [x] Every required acceptance item has objective PASS evidence.
- [x] Every required deterministic command passed on the recorded commit/worktree.
- [x] Source, prior-phase behavior, and file-boundary invariants are verified.
- [x] Completion and rendered evidence reports are present if required.
- [ ] The implementation-readiness manifest passes on the recorded implementation commit and clean worktree. **Not this session’s gate** (`verify_phase_readiness.py` is not the item stopping condition).
- [x] Independent QA handoff is recorded; QA itself remains pending. **Not this session’s stopping condition.** Handoff: `harness/verify/2026-09-08-stage2-bl-012-017-018-independent-qa-handoff.md`.
- [x] No later phase has begun.

Independent QA PASS, master PASS, and the phase seal belong to later gates and are not implementation-task Definition of Done items.

## Unknowns, conflicts, and blockers

- Unknown runtime inputs: none for these presentation items
- Documentation conflicts: `GOALS.md` / `PLANS.md` still say Stage 2+ unauthorized as of Stage 1 P1 master; this session is separately authorized by the Stage 2 launch contract. Do not treat the older index sentence as blocking this authorized item set.
- Blocked work / required direction: none

## Recovery protocol

1. Preserve the current worktree and logs; do not reset or discard evidence.
2. Record the failed/interrupted command, exit state, affected files, and last verified milestone.
3. Inspect exact `HEAD`, status, and diff before resuming.
4. Revalidate any artifact that may be partial or stale.
5. Resume from the last verified milestone only within the authorized boundary; request direction if authority or requirements are unresolved.
6. Before declaring a blocker, make and log at least three materially different safe recovery attempts using the blocker template.
7. Continue other meaningful in-scope work while the affected command is recoverably unavailable.

## Non-blocking failures

Failing/missing tests, incomplete coverage/milestones, compiler/build/lint/typecheck failures, generated drift, runtime paths, occupied ports, temporary locks, caches, dependency materialization, dirty implementation files, missing reports, and normal response-turn boundaries require repair and continuation.

## Allowed stopping conditions

- The verification summary records PASS for BL-012, BL-017, and BL-018 and frontend test/typecheck/lint/build pass; or
- a completed blocker record proves three materially different recovery attempts failed, no meaningful in-scope work remains, and new authority, a product decision, external state, or prohibited scope expansion is required.

## Gate state

- Implementation: complete on dirty HEAD `2f0f4a9` (uncommitted Stage 2 tree)
- Durable goal: complete (stopping condition met)
- Implementation-readiness verifier: not used as item gate
- Independent QA: not this session
- Master decision: not this session
- Seal status: N/A (no Phase 8)
