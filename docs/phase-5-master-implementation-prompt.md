# Phase 5 master implementation prompt

This prompt is self-contained for a future coding session. It is executable only after the user separately and explicitly authorizes Phase 5 implementation and resolves every blocking decision in `docs/phase-5-planning-and-implementation-contract.md`, section 15. Planning authorization on 2026-08-26 is not implementation authorization.

## Ordered instructions

1. Read `AGENTS.md`, `PROJECT_CONTEXT.md`, `docs/current-status.md`, `docs/implementation-plan.md`, `docs/architecture.md`, `docs/data-model.md`, `docs/phase-1-completion-report.md`, `docs/phase-4-master-gate-decision-2026-08-26.md`, and `docs/phase-5-planning-and-implementation-contract.md` in order. Verify the approved HEAD/worktree and stop if governance says implementation is not authorized.
2. Implement only Phase 5 conceptual Wi-Fi visualization and area statistics. Existing-pole mode is mandatory. Never create, move, redistribute, optimize, or delete customer poles. Do not begin Phase 6.
3. Preserve source uploads byte-for-byte, source pole IDs, raw coordinate text, source coordinates, camera priority areas, lighting calculation areas/results, CAP placeholders, and all frozen catalogs. Store Wi-Fi settings and results separately.
4. Add strict typed Wi-Fi configuration, separate `wifi_analysis_areas`, and separate `wifi_coverage` result/state models. Migrate losslessly from project schema 2.4.0 using the approved version rationale. Do not infer an analysis boundary from source extents or other phase polygons.
5. Apply the approved hierarchy: project default radius, nullable per-pole override, nullable per-pole enabled override, explicit clear/restore, notes, configuration revision, and bulk set/clear semantics. An explicit fixture-model replacement starts a new capability-specific baseline and does not carry incompatible Wi-Fi configuration across models; validate fixture type/model consistency atomically. Only WIFI and SMART fixtures with valid Wi-Fi capability may participate; LITE never contributes.
6. Build deterministic projected-plane circle geometry using the approved fixed approximation resolution, finite checks, canonical ring ordering, stable `wifi-circle/<pole_id>` IDs, explicit `projected_ring`, and separately transformed WGS84 display rings. Use the effective coordinate: source unless an explicitly authorized location edit exists, while preserving/recording the source coordinate. Never calculate distances or areas in WGS84 degrees.
7. Calculate individual circle area, no-double-count union, aggregate pairwise overlap, aggregate multiply-covered union, optional analysis-area covered/uncovered area, and boundary line coverage exactly as approved. Define `multiply_covered_union_area_m2` as the area of the unary union of all non-empty candidate-pair intersections above `1e-8 m²`. MVP does not persist pair geometries, maximum overlap multiplicity, or an exact multiplicity histogram; both multiplicity outputs are deferred unless separately approved with their own performance contract. Handle empty/no-boundary/invalid cases explicitly; invalid user-created or edited areas return 422 and never become persisted input.
8. Enforce approved safety limits and deterministic failures for adversarial radii, circles, vertices, non-finite values, invalid topology, and missing/invalid CRS. Use a spatial index for candidate pairs and cap candidate-pair/intersection operations at 50,000; exceeding the cap returns controlled 422 and preserves the prior project/result. With the recommended caps, 500 circles × 129 closed-ring vertices = 64,500 circle vertices, below the 250,000 total geometry-vertex limit; the supplied 74-pole project has at most 2,701 unordered pairs. Never silently simplify, drop geometry, change radii, or return partial results.
9. Add a canonical SHA-256 Wi-Fi input fingerprint over exact effective geometry inputs only. Exclude notes, `modified_at`, and general configuration revision; show notes live from current configuration and do not copy them into calculated-result provenance. Invalidate/remove stale results on source origin/effective coordinate, fixture type/model/capability, active state, project CRS, default radius, per-pole radius/enabled changes, and analysis-area changes. Ensure GET/save/open/bulk/UI mutation paths cannot display stale results.
10. Add API seams using the existing FastAPI/Pydantic/error patterns: typed save/open/bulk behavior and a calculate endpoint returning the complete project. Preserve 404/409/422 semantics and source integrity on failures.
11. Update typed frontend transport and controls. Add project/per-pole/bulk/analysis-area controls, effective-value/provenance display, explicit conceptual disclaimer, empty boundary state, warnings, and safe redraw behavior. Add separate map sources/layers. Preserve fixture colors exactly: LITE red, WIFI yellow, SMART blue; conceptual Wi-Fi must use a distinct overlay color.
12. Keep updated KML export limited to its existing source/fixture-edit contract unless a separate export authorization exists. Do not export conceptual circles as customer poles.
13. Regenerate `schemas/project.schema.json` and `schemas/openapi.json` from the authoritative backend models and verify they are fresh. Do not change frozen catalog schemas/data or Input files.
14. Add complete backend unit/API/migration tests, frontend rendered tests, and supplied-input integration coverage using the 74-pole KML. Include synthetic disjoint, overlapping, triple-overlap, exact multiply-covered-union, clipping, empty, invalid, stale, 50,000-candidate-pair cap, 2,701-pair supplied-input, limit, and migration cases.
15. Run backend tests, engineering-data validation, frontend rendered tests, strict TypeScript, lint, production build, `git diff --check`, and a production-rendered 74-pole workflow. Confirm zero new browser-console errors and exact source byte/coordinate preservation.
16. Before handoff, verify the diff contains only authorized Phase 5 implementation and generated-contract changes, no runtime data/caches/node_modules/Input edits, and no Phase 6 behavior. Request an independent QA review; do not claim verified RF coverage, performance, capacity, service quality, or standards compliance.

## Required implementation handoff

Report the approved decisions, schema/software/model versions, changed files, migration evidence, fingerprint invalidation cases, numerical limits, test commands/results, rendered workflow evidence, source-integrity checks, remaining warnings, and the independent QA gate status. If any blocking decision is unresolved, stop without implementation and ask the user for resolution.
