# Stage 2 launch — BL-012, BL-017, BL-018

**Next action (2026-09-08):** Independent QA of Stage 2. Implementation is committed. Do not start Stage 3. Do not merge.

| Field | Value |
|---|---|
| Branch | `codex/bl-006-ies-associations` |
| Implementation commit | `eb3a606253d794bf8ef685eb69d6c885368823ea` |
| Parent/base | `2f0f4a9c42a3a7bdbb51971591aaced5c57a8d38` |
| QA launch prompt | this file, section **Independent QA launch prompt** |
| QA handoff | `harness/verify/2026-09-08-stage2-bl-012-017-018-independent-qa-handoff.md` |
| QA review output | `harness/verify/2026-09-08-stage2-bl-012-017-018-independent-qa-review.md` |
| Prior gate | Stage 1 P1 CONDITIONAL PASS `docs/post-roadmap-stage1-p1-scoped-master-decision-2026-09-08.md` |

The implementation launch prompt below is historical. Use the Independent QA prompt for the next session.

## Historical implementation launch prompt (do not reuse)

```text
Implement authorized Stage 2 items BL-012, BL-017, and BL-018 only.

Create and activate a durable goal: complete those three items with recorded
verification on this checkout. Continue across turns until
`harness/verify/2026-09-08-stage2-bl-012-017-018-verification.md` records PASS
for all three and frontend test/typecheck/lint/build pass. Independent QA
and merge are not this session's stopping condition.

Read AGENTS.md and its startup sequence, then:
- docs/superpowers/plans/2026-09-08-stage2-bl-012-017-018.md (this contract)
- docs/web-app-review-2026-09-08.md WA-05, WA-10, WA-11
- docs/post-roadmap-backlog.md
- docs/post-roadmap-stage1-p1-scoped-master-decision-2026-09-08.md
- exact git status, HEAD, and diff

Start from commit 2f0f4a9 on codex/bl-006-ies-associations. Preserve unrelated
user-owned changes. Create the work record from
harness/templates/phase-work-record-template.md at
harness/phases/2026-09-08-stage2-bl-012-017-018.md and log commands under
harness/logs/.

Authorized boundary: frontend CSS/components/lib/tests for the three items;
harness work record, execution log, and verification summary; backlog status notes
only. No backend engines, schemas, Input/, catalogs, lockfiles, merge, push,
Phase 8, BL-013, BL-014, BL-016, BL-019, BL-020.

Reproduce each issue before patching. Confirm the card-anchor coordinate
chain; do not treat the double-offset hypothesis as proven until measured.
Settle BL-018 only as the discipline switcher defined in this contract — not
a shell rewrite.

Failing tests, missing tests, lint/typecheck/build failures, dirty in-scope
files, and response-turn boundaries are repair work. Stop early only with a
blocker record after three materially different safe recovery attempts.

Do not commit unless the user asks. Do not merge. Do not start Stage 3.
```

## Independent QA launch prompt (copy this)

> Paste into a **new Independent QA session** on another computer after `git fetch` and checkout of `codex/bl-006-ies-associations`. Controlling details stay in this file. Do not start Stage 3 from chat memory.

```text
Independently review authorized Stage 2 items BL-012, BL-017, and BL-018 only.

Create and activate a durable goal: complete independent QA of those three
items on the recorded implementation commit. Continue across turns until
`harness/verify/2026-09-08-stage2-bl-012-017-018-independent-qa-review.md`
records a supported PASS or FAIL for all three. Do not implement product
fixes in this session. Do not merge, push, start Stage 3, or create a seal.

You did not implement this work. Treat implementer verification as a claim
set, not proof. Use `harness/templates/qa-review-template.md`.

Read AGENTS.md and its startup sequence, then:
- harness/verify/2026-09-08-stage2-bl-012-017-018-independent-qa-handoff.md
- docs/superpowers/plans/2026-09-08-stage2-bl-012-017-018.md
- docs/web-app-review-2026-09-08.md WA-05, WA-10, WA-11
- docs/post-roadmap-backlog.md
- docs/post-roadmap-stage1-p1-scoped-master-decision-2026-09-08.md
- harness/phases/2026-09-08-stage2-bl-012-017-018.md
- exact git status, HEAD, and diff

Implementation identity: commit
eb3a606253d794bf8ef685eb69d6c885368823ea
on branch `codex/bl-006-ies-associations`.
Parent/base: 2f0f4a9c42a3a7bdbb51971591aaced5c57a8d38.
Confirm `git merge-base --is-ancestor eb3a606253d794bf8ef685eb69d6c885368823ea HEAD`
exits 0. Review the Stage 2 product diff of `2f0f4a9..eb3a606`. HEAD may
include later docs-only commits that save this QA prompt and execution-index
notes; that is not a product change. Prefer a clean worktree. Preserve
unrelated user-owned files. Do not reset, stash, revert, merge, or push.

Re-run on this exact checkout, from `frontend/`:
  corepack pnpm run typecheck
  corepack pnpm run lint
  corepack pnpm run test
  corepack pnpm run build
Also: `git diff --exit-code -- Input` and confirm no engine/schema/catalog/
lockfile edits in `2f0f4a9..eb3a606`.

Live checks must use an isolated production Vinext build of THIS checkout and
an isolated FastAPI project store. Do not reuse leftover UI on :3000/:3012
or API on :8000 as identity evidence. Miracle Mile 74-pole import SHA-256
must be 2f89f9f2be306c18221c643c98d5c1a9abdb6449aab8a77ea4b76b3694e8e328.

Acceptance (measure in the browser; do not trust implementer JSON):

BL-012 / WA-05: card opens beside the polygon finish vertex with a small
offset, clamped inside the map. Map container origin is not page (0,0).
Polygons near all four corners and the center. Pan/zoom, panel collapse,
drag, Reset. Reset returns to the measured anchor. Dragged position is a
session overlay, not project JSON. Confirm the coordinate chain; do not
assume the double-offset story without measuring.

BL-017 / WA-10: exact 0 → `0.00 lx`; null/unavailable → `—`;
0 < x < 0.01 → `<0.01 lx` (same policy on card, map labels, left-panel
area summaries). Ordinary values keep two decimals. Uniformity ratios come
from stored numbers, never formatted lux strings. Stored lux is not
rewritten. If a live tiny-positive grid is not reproducible, say so and
still verify wiring plus unit tests against WA-10 values
0.0000359329 / 0.0000227035 / 0.0000682324.

BL-018 / WA-11: Lighting / Camera / Wi-Fi / CAP switcher at the top of the
left panel. Default Lighting after open/import. Lighting is reachable
without scrolling through CAP. CAP pane still exposes blockers and required
inputs (expandable, not deleted). Report Package stays on the toolbar.
Left panel overflowX is false at 1920 and 1024. Switching disciplines does
not discard an active polygon draft without explicit cancel/save. Collapse
rail still works. Do not treat a missing shell rewrite as a FAIL.

Record exact commands under harness/logs/ and keep screenshots/JSON under
harness/tmp or harness/verify as QA evidence. Failures, missing tests, and
inconclusive live setup are repair of the QA session, not product patches.
Stop early only with a blocker record after three materially different safe
recovery attempts.

Do not commit unless the user asks. Do not merge. Do not start Stage 3.
```

## Item set and settled BL-018 UI

| ID | Review | Type | Required outcome |
|---|---|---|---|
| BL-012 / WA-05 | Lighting card initial position | Bug | Card opens beside the polygon finish vertex, with a small offset, clamped inside the map. Reset returns to that anchor. Dragged position stays a session overlay, not project JSON. |
| BL-017 / WA-10 | Tiny lux shown as `0.00` | Presentation | Distinguish exact 0, null/unavailable, and positive values below display precision. Example: `<0.01 lx` for positive `< 0.01`. Do not rewrite stored lux or compute ratios from formatted strings. Apply to the lighting card, map labels, and left-panel area summaries. |
| BL-018 / WA-11 | Left-panel overload and horizontal clip | Bounded UI | **Settled scope (required before coding):** add a Lighting / Camera / Wi-Fi / CAP switcher at the top of the left panel. Default **Lighting** after a project is open. Each pane shows that discipline’s everyday controls; CAP required inputs and blockers stay fully available in the CAP pane (expandable, not deleted). Report Package stays on the toolbar; do not bury lighting drawing under the CAP form. Fix left-panel horizontal clipping (`overflow-x` / `min-width: 0` / full-width controls). Do not rebuild the three-column workspace, right inspector, or map chrome. |

## Non-goals

- BL-013 (multiple result cards / revisit list)
- BL-014 (result-first CAP workflow)
- BL-016 (pixel-density map)
- BL-019 / BL-020 / OBS-01
- Merge to `main`, push, Phase 8, any seal
- Engine, schema, `Input/`, catalog, or lockfile changes
- Invented CAP operational values or azimuths
- Persisting panel/card layout in project JSON

## Likely files (confirm on checkout)

- `frontend/app/components/LightingResultCard.tsx` — current `map.project` minus `container.left/top` is the WA-05 suspect; verify the full chain
- `frontend/app/lib/lighting-card-position.mjs` + `frontend/tests/lighting-card-position.test.mjs`
- `frontend/app/lib/lighting-labels.mjs` + `frontend/tests/lighting-labels.test.mjs` — labels already have `formatLux`; card and area-row `toFixed(2)` still hide tiny positives
- `frontend/app/components/LightingResultCard.tsx` and `EngineeringWorkspace.tsx` area summaries
- `frontend/app/components/EngineeringWorkspace.tsx` left `aside.side-panel` (CAP + Report currently precede Layers/Lighting)
- `frontend/app/globals.css` — `.side-panel`, `.panel-scroll`, section/switcher layout
- Tests: extend existing helpers; add a left-panel layout test; keep frontend suite green

## Acceptance

- **BL-012:** Polygons near all four map corners and center; map container not at page origin `(0,0)`; pan/zoom, panel collapse, drag, Reset. Measure card-to-anchor in the browser.
- **BL-017:** Exact 0 → `0.00 lx`; null → `—`; `0 < x < 0.01` → `<0.01 lx` (or the same policy everywhere, documented in the verification file); ordinary values keep two-decimal display. Uniformity ratios still come from stored numbers.
- **BL-018:** After open, Lighting pane is reachable without scrolling through CAP. CAP pane still exposes blockers and required inputs. Left panel `overflowX` is false at 1920 and 1024. Switching disciplines does not discard an active polygon draft without an explicit cancel/save. Collapse rail still works.

## Verification (final readiness)

From repo root / `frontend/` as appropriate:

```powershell
corepack pnpm run typecheck
corepack pnpm run lint
corepack pnpm run test
corepack pnpm run build
```

Plus live browser: Miracle Mile 74-pole import, SHA-256 `2f89f9f2be306c18221c643c98d5c1a9abdb6449aab8a77ea4b76b3694e8e328`; card anchor measurement; a result with tiny positive lux if reproducible; left-panel switcher and overflow. Record exact commands in `harness/logs/` and the verification summary.

## Proven-blocker rule

Ordinary failures are repaired in-session. Stop and ask only when the same condition still blocks after three materially different safe recoveries and needs new authority, a product decision, an external-state change, or a prohibited scope expansion (including widening BL-018 into a shell rewrite).
